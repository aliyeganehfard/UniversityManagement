package model.repository.course;

import model.entity.Course;
import model.repository.RepositoryImpl;

public class CourseRepositoryImpl extends RepositoryImpl<Course,Integer> implements CourseRepository{
}

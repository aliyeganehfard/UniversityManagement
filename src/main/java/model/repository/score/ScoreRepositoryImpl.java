package model.repository.score;

import model.entity.Score;
import model.repository.Repository;
import model.repository.RepositoryImpl;

public class ScoreRepositoryImpl extends RepositoryImpl<Score,Integer> implements ScoreRepository {
}
